import React from 'react';
import PropTypes from 'prop-types';

camera.propTypes = {
    
};

function camera(props) {
    return (
        <div>
            
        </div>
    );
}

export default camera;